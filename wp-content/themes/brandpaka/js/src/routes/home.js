import menuScrollSpy from '../modules/menu-scroll-spy';

export default {
  init() {
    menuScrollSpy();
  },
  finalize() {
    // JavaScript to be fired on the home page, after the init JS
  },
};
