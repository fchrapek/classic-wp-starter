const stickyNav = () => {
  // const siteHeader = document.querySelector('.site-header');

  // let lastScrollTop = 0;

  // const stickyNavBehaviour = () => {
  //   const scrollTop = window.scrollY || document.documentElement.scrollTop;

  //   if ((scrollTop > lastScrollTop) && window.pageYOffset > 100) {
  //     siteHeader.classList.add('-scrolled');
  //     siteHeader.classList.remove('-scrolled-up');
  //   } else {
  //     siteHeader.classList.add('-scrolled-up');
  //   }

  //   lastScrollTop = scrollTop <= 0 ? 0 : scrollTop;

  //   if (window.pageYOffset <= 5) {
  //     siteHeader.classList.add('-scolled-to-the-top');
  //     siteHeader.classList.remove('-scrolled-up');
  //     siteHeader.classList.remove('-scrolled');
  //   } else {
  //     siteHeader.classList.remove('-scolled-to-the-top');
  //   }
  // };

  // document.addEventListener('scroll', stickyNavBehaviour);
};

export default stickyNav;
